pipeline {
    agent any
    stages{
        stage('Benjamin Gambill - Build Docker Image'){
            steps{
                sh './gradlew build --no-daemon'
            }
        }
        
    }
}
